{
  "name": "car-gui-backend",
  "version": "1.0.0",
  "description": "This is the backend for writing to the serialport of the microcontroller",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Loren Anderson",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "serialport": "^8.0.6",
    "socket.io": "^2.3.0"
  }
}
